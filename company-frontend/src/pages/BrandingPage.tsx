import { useEffect, useState, type ChangeEvent, type CSSProperties, type FormEvent } from "react";
import { ApiMessage, ErrorState, Loading, PageHeader, errorText, formObject } from "../components/Ui";
import { put } from "../lib/api";
import { text } from "../lib/format";
import { useApiData } from "../lib/useApiData";

export function BrandingPage(){const {data,loading,error,refresh}=useApiData<Record<string,unknown>>("/organization");const [preview,setPreview]=useState<Record<string,string>>({});const [message,setMessage]=useState("");const [formError,setFormError]=useState("");const [pending,setPending]=useState(false);useEffect(()=>{if(data)setPreview(Object.fromEntries(Object.entries(data).map(([k,v])=>[k,text(v,"")])));},[data]);if(loading)return <Loading label="Loading branding"/>;if(error)return <ErrorState message={error} retry={refresh}/>;const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setPending(true);setFormError("");try{await put("/branding",formObject(e));setMessage("Branding settings saved.")}catch(err){setFormError(errorText(err))}finally{setPending(false)}};const update=(e:ChangeEvent<HTMLInputElement|HTMLTextAreaElement>)=>setPreview((p)=>({...p,[e.target.name]:e.target.value}));return <><PageHeader eyebrow="VISITOR EXPERIENCE" title="Branding" description="Configure how your organization is introduced in customer-facing guides."/><ApiMessage error={formError} success={message}/><div className="branding-grid"><section className="panel form-panel"><form className="form-grid" onSubmit={submit}><label className="span-2">Display name<input name="displayName" defaultValue={text(data?.displayName,"")} onChange={update} required/></label><label className="span-2">Logo URL<input name="logoUrl" type="url" defaultValue={text(data?.logoUrl,"")} onChange={update} placeholder="https://…"/></label><label>Primary color<input name="primaryColor" type="color" defaultValue={text(data?.primaryColor,"#173f3b")} onChange={update}/></label><label>Secondary color<input name="secondaryColor" type="color" defaultValue={text(data?.secondaryColor,"#e8684a")} onChange={update}/></label><label className="span-2">Welcome message<textarea name="welcomeMessage" rows={4} defaultValue={text(data?.welcomeMessage,"")} onChange={update}/></label><label>Support email<input name="supportEmail" type="email" defaultValue={text(data?.supportEmail,"")} onChange={update}/></label><label>Support phone<input name="supportPhone" type="tel" defaultValue={text(data?.supportPhone,"")} onChange={update}/></label><div className="form-actions span-2"><button className="btn btn--primary" disabled={pending}>{pending?"Saving…":"Save branding"}</button></div></form></section><aside><span className="preview-label">VISITOR PREVIEW</span><div className="visitor-preview" style={{"--brand-primary":preview.primaryColor||"#173f3b","--brand-accent":preview.secondaryColor||"#e8684a"} as CSSProperties}><header>{preview.logoUrl?<img src={preview.logoUrl} alt="Organization logo preview"/>:<span>{(preview.displayName||"Y").charAt(0)}</span>}<strong>{preview.displayName||"Your travel company"}</strong></header><main><small>WELCOME TO YOUR PARK DAY</small><h2>Hello, traveler.</h2><p>{preview.welcomeMessage||"Your company welcome message will appear here."}</p><div className="preview-next"><span>UP NEXT</span><strong>Your personalized itinerary</strong><button type="button">View next recommendation</button></div></main><footer>Need help? {preview.supportEmail||preview.supportPhone||"Support contact"}</footer></div><p className="preview-help">Preview only. Saved settings depend on visitor-experience API support.</p></aside></div></>}
